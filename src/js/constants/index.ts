const dataList:HTMLUListElement = document.getElementById('dataList') as HTMLUListElement
const btnFetch:HTMLInputElement = document.getElementById("btn-search") as HTMLInputElement
const inputSerch:HTMLInputElement = document.getElementById("btn-search") as HTMLInputElement
const statusBar:HTMLSpanElement = document.getElementById("status-bar") as HTMLSpanElement


export {
    dataList,
    btnFetch,
    inputSerch,
    statusBar
}